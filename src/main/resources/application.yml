server:
  port: 8080
  servlet:
    context-path: /api-strucyourlife
spring:
  application:
    name: Backend Struc Your Life
  datasource:
    url: jdbc:postgresql//localhost:5432/strucyourlife?currentSchema=strucyourlife
    driver-class-name: org.postgresql.Driver
    username: therivex1907
    password: root
  jpa:
    properties:
      hibernate:
        ejb: interceptor-org
        format_sql: true
        globally_quoted_identifiers: true
  logging:
    level:
      org:
        hibernate:
          SQL: DEBUG